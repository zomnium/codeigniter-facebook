=========================================
	CodeIgniter Facebook API Library
=========================================

Just started developing, for now it's just a merged file of the original Facebook php API. The only modification is the constructor, so that it's easier to use within CodeIgniter. Also a configuration file is added. In the near future helpers, CI_session and autoload support will be added.